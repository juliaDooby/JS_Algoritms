---------------------------------------------------------------------------------------------
⋙ ❍ Задача LCS:
---
5️⃣ ⌛ Задача на LCS для двух строк с пробеламЗадача Найдите LCS двух строк, которые содержат пробелы.
🎯 Решение:
```js
function lcs(str1, str2) {
  const m = str1.length, n = str2.length;
  const dp = Array(m + 1).fill().map(() => Array(n + 1).fill(0));

  for (let i = 1; i <= m; i++) {
    for (let j = 1; j <= n; j++) {
      if (str1[i - 1] === str2[j - 1]) {
        dp[i][j] = dp[i - 1][j - 1] + 1;
      } else {
        dp[i][j] = Math.max(dp[i - 1][j], dp[i][j - 1]);
      }
    }
  }

  return dp[m][n];
}

console.log(lcs("hello world", "helloworld")); // 10
```Объяснение Пробелы считаются символами, и они участвуют в поиске общей подпоследовательности.
---------------------------------------------------------------------------------------------
